import React from 'react';

export default function PrivateRoute() {
 return (
  <h2>
   You can not view this in the browser as the folder is named with underscore
   making that route private
  </h2>
 );
}
